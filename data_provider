def import (file_name):
    return (list_of_dictionary) 
# читаем информацию из файла (можно путь до файла) и возвращаем массив словарей 
# типы файлов c БД: .csv, .json, .xml, .yaml

def export (list_of_dictionary, file_name, format): # format: .csv, .json, .xml, .yaml
    return (status) # возвращает статус: ок или не ок, если такая запись есть
file.write() # запись в файл
